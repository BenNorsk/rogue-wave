<div class="rw-second-background">
  <%= render "shared/title_wave", element: "My Bookings" %>
  <div>
    <div class="all-booking-cards">
      <% @bookings.each do |booking| %>
      <div class="p-2">
        <%= render "shared/booking_card", element: booking %>
      </div>
      <% end %>
    </div>
  </div>
</div>

